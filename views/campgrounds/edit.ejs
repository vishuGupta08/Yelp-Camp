<% layout('layouts/boilerplate') %>

<body>
    <h1>Edit Details</h1>
    <form action="/campgrounds/<%= campgrounds._id%>?_method=PUT" method="POST">
        <div>
            <label for="1">Enter Title:</label>
            <input type="text" name="campground[title]" id="1" value="<%= campgrounds.title%>">
        </div>
        <div>
            <label for="2">Enter Location</label>
            <input type="text" name="campground[location]" id="2" value="<%= campgrounds.location%>">
        </div>

        <button>Submit</button>

    </form>
    <a href="/campgrounds/<%=campgrounds._id%>">Back</a>
    <a href="/campgrounds">Home Page</a>